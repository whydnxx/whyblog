export type Meta = {
  title: string;
  description: string;
  image: string;
  type: string;
  date?: string;
  siteName: string;
  siteUrl: string;
};

export type Navigation = {
  label: string;
  url: string;
};
